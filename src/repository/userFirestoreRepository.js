// Importar DAO de UserFirestore

class UserFirestoreRepository
{
     async create(data)
     {
        // Crear el user dentro de firestore
     }

     async get(id)
     {
        // Devolver el user de firestore
        // return new UserEntity(userDto);
     }
}

export default UserFirestoreRepository;
