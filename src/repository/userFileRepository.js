// Importar DAO de UserFile

class UserFileRepository
{
     async create(data)
     {
        // Crear el user dentro de un archivo
     }

     async get(id)
     {
        // Devolver el user de un archivo
     }
}

export default UserFileRepository;
